import { AgendaOrderPipe } from './agenda-order.pipe';

describe('AgendaOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new AgendaOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
