import { FilterVersionUploadsPipe } from './filter-version-uploads.pipe';

describe('FilterVersionUploadsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVersionUploadsPipe();
    expect(pipe).toBeTruthy();
  });
});
