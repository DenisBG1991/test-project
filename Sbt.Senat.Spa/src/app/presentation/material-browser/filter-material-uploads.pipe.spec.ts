import { FilterMaterialUploadsPipe } from './filter-material-uploads.pipe';

describe('FilterMaterialUploadsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMaterialUploadsPipe();
    expect(pipe).toBeTruthy();
  });
});
